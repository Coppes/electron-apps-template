/**
 * Update the dock/taskbar badge
 * @param {string} text
 */
export declare function setDockBadge(text: any): void;
/**
 * Update the dock menu (macOS only) or JumpList (Windows)
 * @param {Array} template
 */
export declare function setDockMenu(template: any): void;
