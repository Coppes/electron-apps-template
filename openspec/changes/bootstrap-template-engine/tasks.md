# Tasks for bootstrap-template-engine

1. Scaffold main/renderer separation with TypeScript in both processes
2. Integrate Vite.js for renderer with HMR
3. Configure auto-reload for main process (nodemon)
4. Implement secure IPC via contextBridge and preload
5. Add example IPC: window.api.getVersion()
6. Set up electron-builder for Win/Mac/Linux packaging
7. Add app icons and build scripts
8. Enforce security defaults (contextIsolation, nodeIntegration, CSP)
9. Integrate electron-store for persistence
10. Implement frameless window and custom titlebar (optional)
11. Install and configure shadcn/ui kit (Button, Resizable, ThemeToggle, Menu, Sheet, Dialog, Toaster, Input, Select, Switch, Slider, Table, Tooltip)
12. Create App Shell layout (Sidebar + Main Content)
13. Create Settings page (with electron-store persistence)
14. Create About page (showing IPC version info)
15. Implement native file opener example (IPC + dialog + file read)
16. Validate implementation with tests and documentation
